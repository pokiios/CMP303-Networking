using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Constants
{
    public const int TICKS_PER_SEC = 30; // Number of ticks per second
    public const int MS_PER_TICK = 1000 / TICKS_PER_SEC; // Length of a tick in milliseconds
}
